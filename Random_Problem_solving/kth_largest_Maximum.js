const arr=[1,23,12,9,302,50];


const k=3;


function kthLargestMaximum(arr,k)
{
    let max=-Infinity;
   let queue=[];
   for(let i=0;i<arr.length;i++)
   {
          if(arr[i]>max)
          {
            queue.push(arr[i]);
          }

          if()
   }
}